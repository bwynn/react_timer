const React = require('react');

const Countdown = React.createClass({
  render: function () {
    return (
      <div>
        <p>Countdown.jsx</p>
      </div>
    );
  },
});

module.exports = Countdown;
